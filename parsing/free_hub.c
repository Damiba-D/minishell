#include "minishellM.h"

char	*remove_all_reds(char *seg)
{
	char	*cleaned;
	int		src_i;
	int		dst_i;

	src_i = 0;
	dst_i = 0;
	cleaned = malloc(ft_strlen(seg) + 1);
	if (!cleaned)
		return (NULL);
	while (seg[src_i])
	{
		
	}
}